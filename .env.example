# ── Frontend (Vite) — se exponen al browser ──────────────────────────
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# VAPID public key para Web Push (seguro de exponer)
VITE_VAPID_PUBLIC_KEY=

# ── Servidor (Netlify Functions) — NO commitear valores reales ───────
# SUPABASE_SERVICE_ROLE_KEY=          # solo en Netlify
# VAPID_PUBLIC_KEY=                   # mismo valor que VITE_VAPID_PUBLIC_KEY
# VAPID_PRIVATE_KEY=                  # secreto — solo en Netlify
# VAPID_SUBJECT=mailto:gerente091@gmail.com
# WEBHOOK_SECRET=                     # secreto compartido con el webhook de Supabase
